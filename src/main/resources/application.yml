logging:
  level:
    root: info

server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/antonfifindik/spring-cloud-config-git-repo.git
    bus:
      enabled: true


  rabbitmq:
    host: 192.168.99.100
    port: 5672
    username: guest
    password: guest